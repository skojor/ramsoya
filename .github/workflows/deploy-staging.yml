name: Deploy to Staging

on:
  push:
    branches:
      - staging

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy via SCP
        uses: appleboy/scp-action@v0.1.7
        with:
          host: homer.skorstad.name
          username: jskorstagh
          key: ${{ secrets.SSH_KEY }}
          port: 22
          source: "."
          target: "web/"

