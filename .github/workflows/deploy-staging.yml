- name: Deploy via SCP
  uses: appleboy/scp-action@v0.1.7
  with:
    host: ${{ secrets.SERVER_IP }}
    username: youruser
    key: ${{ secrets.SSH_KEY }}
    port: 22
    source: "."
    target: "web/"

